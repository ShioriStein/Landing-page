const baseService = "/api/v1/companies";

const COMPANY = {
  GET: `${baseService}`,
  ADD: `${baseService}`,
  DELETE: `${baseService}`,
  GET_BY_ID: `${baseService}`,
  UPDATE: `${baseService}`
};

export default COMPANY;
