const baseService = "/api/v1/tasks";

const TASKS = {
  CREATE: `${baseService}`,
  GET: `${baseService}`,
  GET_BY_ID: `${baseService}`,
  GET_PERSONAL: `${baseService}/personal`
};

export default TASKS;
