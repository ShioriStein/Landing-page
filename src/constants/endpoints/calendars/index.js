const baseService = "/api/v1/Calendars";

const CALENDARS = {
  GET: `${baseService}`,
  ADD: `${baseService}`,
  DELETE: `${baseService}`,
  GET_BY_ID: `${baseService}`,
  UPDATE: `${baseService}`
};

export default CALENDARS;
