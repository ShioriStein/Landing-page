const baseService = "/api/v1/roles";

const ROLES = {
  GET: `${baseService}`,
  ADD: `${baseService}`,
  DELETE: `${baseService}`,
  GET_BY_ID: `${baseService}`,
  UPDATE: `${baseService}`
};

export default ROLES;
